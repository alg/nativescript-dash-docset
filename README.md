# NativeScript Dash Docset script and assets

This is the repo with the shell script and assets to make Dash docset generation simple.

To run it:

    $ ./generate-docset
    
The resulting docset will be put in `nativescript.docset`.
